# zizmor policy for jooservices/laravel-events workflows.
# Version tags (@v7) are acceptable; SHA pinning is enforced selectively.
rules:
  unpinned-uses:
    config:
      policies:
        "*": ref-pin
