# Ignore files

The repository ignores generated dependencies, local environment files, test
caches, build output, IDE metadata, logs, and operating system metadata.

`composer.lock` is currently ignored in this package. DTO tracks a lock file,
so this will be reviewed during tooling alignment before final validation.
