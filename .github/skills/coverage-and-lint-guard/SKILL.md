# Coverage And Lint Guard

Use `composer lint`, `composer lint:all`, `composer test`,
`composer test:coverage`, `composer check`, and `composer ci`.

Pint wins formatting conflicts. Storage behavior requires real MongoDB
integration tests.
