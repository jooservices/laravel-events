# CI triage prompt

Inspect the failing workflow, exact job logs, branch, and changed files. Do not
guess. Prefer fixing the root cause locally with the same Composer command used
by CI. Report external blockers with exact command output.
