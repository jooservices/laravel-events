# Package change prompt

Inspect the current code, docs, Composer scripts, CI, and tests before changing
files. Keep the change inside `jooservices/laravel-events` scope: Laravel 12
event sourcing and audit event log persistence for MongoDB.

Run relevant Composer checks and update tests/docs for behavior changes.
