# Security review prompt

Review payload, metadata, audit snapshots, redaction, retention, authorization
boundaries, secrets in docs/tests, and dependency/workflow changes. Application
authorization remains outside package scope.
