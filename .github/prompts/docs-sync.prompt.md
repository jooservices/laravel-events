# Docs sync prompt

Keep docs in the current DTO-style tree under `docs/00-architecture` through
`docs/05-maintenance`. Risks and docs changelog belong in `docs/05-maintenance`.
Do not add `docs/05-ai-contributor-guide`.
